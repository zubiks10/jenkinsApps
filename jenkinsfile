pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install' // Example Maven build step
            }
        }
        // Add other stages and steps as needed
    }
}
